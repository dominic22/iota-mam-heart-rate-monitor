{
  "name": "heart-rate-backend",
  "version": "1.0.0",
  "description": "Heart rate backend which publishes received heart rates to the tangle.",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "netlify-lambda build src",
    "start": "nodemon src/index.js"
  },
  "author": "dominic22",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "netlify-lambda": "^1.6.3",
    "nodemon": "^2.0.1"
  }
}
